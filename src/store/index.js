import { createStore } from "vuex";
import location from "@/store/location";
export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    location,
  },
});
